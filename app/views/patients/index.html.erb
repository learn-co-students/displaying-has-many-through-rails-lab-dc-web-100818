<h1>Patients Index Page</h1>
<br>
<ul>
  <% @patients.each do |pat| %>
    <li>Name: <%=link_to pat.name, patient_path(pat) %></li>
    <li>Number of Appointments: <%= pat.app_count %></li> <br>
  <% end %>
</ul>
